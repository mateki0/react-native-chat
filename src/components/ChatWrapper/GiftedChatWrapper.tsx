import styled from 'styled-components/native';

const GiftedChatWrapper = styled.View`
  margin-top: -35px;
  z-index: 2;
  background-color: #fff;
  border-radius: 35px;
  flex: 1;
`;

export default GiftedChatWrapper;
